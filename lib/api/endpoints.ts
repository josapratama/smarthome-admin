export const API = {
  // AUTH
  login: "/auth/login", // <-- sesuaikan dengan endpoint backend yang sudah ada
  // DASHBOARD
  overview: "/admin/overview", // <-- sesuaikan (atau /stats, /metrics, dll)
} as const;
